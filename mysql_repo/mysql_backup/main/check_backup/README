#################################### MySQL Check ##################################
一、恢复验证逻辑

恢复逻辑
1、从info表获取当前需要验证的index
检测当前

2、检测当前是否恢复的是有效验证集
3、如果不是，开始恢复，同步到验证结果表，更新为Doing状态
2、从结果表获取状态为Doing的结果集
    验证成功更新为Succ
    验证失败更新为Fail
