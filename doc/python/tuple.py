元组

单个元素元组
    >>> a = (3)
    >>> b = (3,) # 需要加逗号
    >>> 
    >>> type(a)
    <type 'int'>
    >>> type(b)
    <type 'tuple'>

用途

    一般认为,元组有这类特点,并且是它使用的情景:
        1)元组比列表操作速度快.如果您定义了一个值的常量集,并且唯一要用它做的是不断地遍历它,请使用元组代替列表.
        2)如果对不需要修改的数据进行 "写保护",可以使代码更安全.使用元组而不是列表如同拥有一个隐含的 assert 语句,说明这一数据是常量.如果必须要改变这些值,则需要执行元组到列表的转换 (需要使用一个特殊的函数).
        3)元组可以在字典(又一种对象类型,后面要讲述) 中被用做 key,但是列表不行.字典的key 必须是不可变的.元组本身是不可改变的,列表是可变的.
        4)元组可以用在字符串格式化中
